import React from 'react'
import { View } from 'react-native'

/**
 * (Bottom sheet || Modal Screen) to show the view of a user
 */
export default function TBUserScreen() {
    return (
        <View>
            
        </View>
    );
}
